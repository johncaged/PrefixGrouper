[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "prefix_grouper"
version = "0.0.0"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["prefix_grouper"]
